# Layout Folder Purpose

- **NOT for styling** like colors or backgrounds
- Used for **structural arrangement** only
- Handles component positioning and spacing
- Controls where things appear on the page
- Examples: creating two-column layouts, sidebars, headers/footers
- Think "where components go" not "how they look"
- Styling (colors, fonts, backgrounds) belongs in UI components or CSS files